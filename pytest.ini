# Test configuration (HA 2026.9 + pytest-homeassistant-custom-component).
# asyncio_mode=auto is required by the HA test plugin's autouse async fixtures.
[pytest]
testpaths = tests
asyncio_mode = auto
